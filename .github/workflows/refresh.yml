name: Refresh NRL ladder

on:
  schedule:
    - cron: '0 */6 * * *'      # every 6 hours
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Fetch latest ladder
        run: node scripts/fetch-ladder.mjs

      - name: Fetch remaining draw
        run: node scripts/fetch-draw.mjs

      - name: Fetch completed results
        run: node scripts/fetch-results.mjs

      - name: Fetch closing odds
        run: node scripts/fetch-odds.mjs

      - name: Run Monte Carlo simulation
        run: node scripts/simulate.mjs

      - name: Compute strength grid
        run: node scripts/compute-strength-grid.mjs

      - name: Compute beating odds
        run: node scripts/compute-beating-odds.mjs

      - name: Commit if data changed
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/ladder.json data/draw.json data/results.json data/forecast.json data/strength-grid.json data/odds.json data/beating-odds.json
          if git diff --cached --quiet; then
            echo "No data changes to commit."
          else
            git commit -m "Refresh data ($(date -u +%Y-%m-%dT%H:%MZ))"
            git push
          fi

      - name: Deploy to Cloudflare
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx --yes wrangler@latest deploy
